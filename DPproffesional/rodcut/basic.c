#include <stdio.h>
#include <limits.h>

int RC( int p[], int n ){

    if( n==0 )
    return 0;

    int q = INT_MIN;
}

int main(){

    int p[] = {3, 5, 1, 7, 8};
    int n = sizeof( p );

    int maxCOST = RC( p, n );

return 0;

}